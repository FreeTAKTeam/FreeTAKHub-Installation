---
# Example hosts.yml
all:
  hosts:
    freetakserver:
      ansible_host: localhost

    webmapserver:
      ansible_host: localhost

    videoserver:
      ansible_host: localhost

    noderedserver:
      ansible_host: localhost
