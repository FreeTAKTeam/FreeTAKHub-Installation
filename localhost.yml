---
# Example hosts.yml
all:
  hosts:
    freetakserver:
      ansible_connection: local

    webmapserver:
      ansible_connection: local

    videoserver:
      ansible_connection: local

    noderedserver:
      ansible_connection: local
